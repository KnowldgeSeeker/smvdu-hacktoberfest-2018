#inlcude<stdio.h>
int main()
{
int num;
printf("Enter a number\n");
scnaf("%d",&num);
int reverse=0;
for(;num!=0;reverse=reverse*10+num%10,num=num/10);
printf("The reverse form of given number is %d",reverse);
return 0;
}
