#include<stdio.h>
int main()
{
int num;
printf("Enter a number=\n");
scanf("%d",&num);
int reverse=0;
for(;num!=0;reverse=reverse*10+num%10,num=num/10);
printf("The reversed form of given number is %d",reverse);
return 0;
}
